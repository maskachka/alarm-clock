#pragma once

#include <lvgl.h>

namespace UiIcon {
constexpr const char* kBack = LV_SYMBOL_LEFT;
constexpr const char* kBackLabel = LV_SYMBOL_LEFT " Back";
constexpr const char* kSettings = LV_SYMBOL_SETTINGS;
}  // namespace UiIcon
